import { StyleSheet, View } from "react-native";
export function TabMail() {
  return <View style={styles.container}>{/* <Text>dd</Text> */}</View>;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#ffffff",
    alignItems: "center",
    justifyContent: "center",
  },
});
